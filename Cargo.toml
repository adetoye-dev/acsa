[workspace]
members = ["core"]
resolver = "2"

[workspace.package]
edition = "2021"
license = "Apache-2.0"
version = "0.1.0"
authors = ["Achsah Systems"]

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = "symbols"
